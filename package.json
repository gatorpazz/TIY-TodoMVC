{
  "private": true,
  "dependencies": {
    "todomvc-app-css": "^2.0.1",
    "todomvc-common": "^1.0.2"
  },
  "scripts": {
    "test": "browser-sync start --server --files='js/**/*.js,test/spec/**/*.js' --startPath test/index.html",
    "start": "browser-sync start --server"
  }
}
